const tech = 'Noje.js';
const array = ['Node.js', 'React', 'Typescript'];

const includes = tech.includes('.js'); // Nao precisa ser valor exato
const includeArr = array.includes('React'); // Valor exato

const startsWith = tech.startsWith('N'); // Comeca com - endsWith segue o mesmo principio

console.log(startsWith);